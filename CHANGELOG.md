# Changelog

All notable changes follow semantic versioning.

## Unreleased

- Compare integer readback values without lossy floating-point coercion.
- Add one-click browser and CLI demos that use bundled, isolated sample data.
- Add declared claim checks, a real 404 page, complete route metadata, and a CSP.
- Force owner-only permissions when replacing the CLI license cache.
- Allow a fresh valid cached license to run batch manifests without a repeated environment token.

## 0.1.0 — 2026-08-28

- Initial Supabase post-apply witness with live and offline readback.
- Human and JSON receipts with conservative field statuses and secret redaction.
- Static documentation and live comparison demo.
- Repair receipt overwrite permissions, private service-worker caching, Azure response policies, mobile touch targets, and Cargo package boundaries after independent release verification.
